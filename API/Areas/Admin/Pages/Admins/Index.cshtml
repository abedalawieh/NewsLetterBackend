@page
@model NewsletterApp.API.Areas.Admin.Pages.Admins.IndexModel
@{
    ViewData["Title"] = "Administrator Management";
}

<div class="d-flex align-items-center mb-5">
    <h2 class="fw-bold mb-0">Administrators</h2>
    <a asp-page="Create" class="btn btn-primary ms-auto">
        <i class="fas fa-plus me-2"></i> Deploy New Admin
    </a>
</div>

<div class="card">
    <div class="table-responsive">
        <table class="table table-dark table-hover align-middle mb-0">
            <thead class="table-active">
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Role</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Administrators?.Any() == true)
                {
                    @foreach (var admin in Model.Administrators)
                    {
                        <tr>
                            <td class="fw-bold">@admin.UserName</td>
                            <td>@admin.Email</td>
                            <td>
                                <span class="badge bg-primary">Administrator</span>
                            </td>
                            <td>
                                @if (admin.IsActive)
                                {
                                    <span class="badge bg-success">Active</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">Inactive</span>
                                }
                            </td>
                            <td>
                                <div class="btn-group btn-group-sm">
                                    @if (!Model.IsSystemAdmin(admin.UserName))
                                    {
                                        <a asp-page="ChangePassword" asp-route-id="@admin.Id" class="btn btn-outline-info" title="Reset Password">
                                            <i class="fas fa-key"></i>
                                        </a>
                                        <button type="button" class="btn btn-outline-warning" 
                                                onclick="document.getElementById('toggleForm-@admin.Id').submit();"
                                                title="Enable/Disable">
                                            <i class="fas fa-power-off"></i>
                                        </button>
                                    }
                                    else if (Model.IsCurrentUserSystemAdmin())
                                    {
                                        <a asp-page="ChangePassword" asp-route-id="@admin.Id" class="btn btn-outline-info" title="Change My Password">
                                            <i class="fas fa-key"></i> Change My Password
                                        </a>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary" title="System admin - only they can change their password">Protected</span>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" class="text-center text-secondary py-4">
                            No administrators found
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>


@if (Model.Pagination?.TotalPages > 1)
{
    <nav aria-label="Page navigation" class="mt-4">
        <ul class="pagination justify-content-center">
            @for (int i = 1; i <= Model.Pagination.TotalPages; i++)
            {
                if (i == Model.Pagination.CurrentPage)
                {
                    <li class="page-item active">
                        <span class="page-link">@i</span>
                    </li>
                }
                else
                {
                    <li class="page-item">
                        <a class="page-link" asp-page="Index" asp-route-pageNumber="@i">@i</a>
                    </li>
                }
            }
        </ul>
    </nav>
}


@foreach (var admin in Model.Administrators)
{
    @if (!Model.IsSystemAdmin(admin.UserName))
    {
        <form id="toggleForm-@admin.Id" method="post" asp-page-handler="ToggleStatus" asp-route-id="@admin.Id" style="display:none;"></form>
    }
}
